# suppress inspection "UnusedProperty" for whole file

server.port=8081
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/bookstore-springboot-api?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=${MYSQL_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

spring.devtools.restart.enabled=true

spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

jwt.secret=hie4YsPd2pcuaA99cd3d6hbs*4k$&z4pGMY
jwt.expiry=259200000

stripe.key=${STRIPE_KEY}
stripe.webhookEndpointSecret=${STRIPE_WEBHOOK_SECRET}
ui.baseUrl=http://localhost:4200

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql=TRACE